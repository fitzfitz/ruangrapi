import { Field } from '@base-ui/react/field'
import { Form as BaseForm } from '@base-ui/react/form'

const Form = BaseForm
const FormField = Field.Root
const FormLabel = Field.Label
const FormDescription = Field.Description
const FormControl = Field.Control
const FormError = Field.Error

export { Form, FormField, FormLabel, FormDescription, FormControl, FormError }
