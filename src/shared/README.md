# shared

Reusable code that is not specific to one domain module belongs here.

Use this area for generic components, shared utilities, infrastructure helpers, and shared TypeScript types when they are introduced.
